import { RenderchildDirective } from './renderchild.directive';

describe('RenderchildDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderchildDirective();
    expect(directive).toBeTruthy();
  });
});
